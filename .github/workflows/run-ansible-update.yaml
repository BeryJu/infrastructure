---
name: Run ansible (update)

on:
  workflow_dispatch:
  push:
    branches: [ master ]

jobs:
  ansible:
    runs-on: self-hosted
    steps:
    - uses: ./.github/actions/run-ansible
      with:
        playbook: actions/update.yml
